resources:
- manager.yaml
- rsynctemplate.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- files:
  - controller_manager_config.yaml
  name: manager-config
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
images:
- name: controller
  newName: arti.dev.cray.com/rabsw-docker-stable-local/nnf-dm
  newTag: 0.0.1
