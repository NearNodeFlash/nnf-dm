resources:
- manager.yaml

generatorOptions:
  disableNameSuffixHash: true

configurations:
- rsync_template_config.yaml

configMapGenerator:
- files:
  - controller_manager_config.yaml
  name: manager-config
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
images:
- name: controller
  newName: arti.dev.cray.com/rabsw-docker-master-local/cray-dp-nnf-dm
  newTag: 0.0.1


